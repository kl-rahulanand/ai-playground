[project]
name = "sentinel"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "kl-rahulanand", email = "rahul.anand@knacklabs.ai" }
]
requires-python = ">=3.14"
dependencies = [
    "anthropic>=1.0.0",
    "matplotlib>=3.11.1",
    "pydantic>=2.13.4",
    "python-dotenv>=1.2.3",
]

[project.scripts]
sentinel = "sentinel:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
